<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="bg-[#111] text-white w-[100%] h-[100vh] p-10">
        <div>
            <a href="/" class="text-blue-500">Go to home</a>
        </div>

        <h1 class="text-3xl font-semibold text-gray-400 mt-10 mb-5 ml-2">All User's Here</h1>
        <div class="flex gap-5 flex-wrap">
            <%if(user.length> 0){%>
                <%user.forEach((val)=>{%>
                    <div class="bg-zinc-700 w-[230px] rounded-lg p-3">
                        <div class="h-60 w-full bg-zinc-900 rounded overflow-hidden object-cover">
                            <img src="<%=val.url%>" class="h-full w-full" alt="_img">
                        </div>
                        <h1 class="text-xl font-semibold capitalize mt-2">
                            <%=val.name%>
                        </h1>
                        <p class="text-zinc-400 lowercase text-xs">
                            <%=val.email%>
                        </p>

                        <div class="flex justify-between items-center mt-5 px-2">
                            <a href="/edit/<%=val._id%>" class="text-zinc-400 font-semibold">Edit</a>
                            <a href="/delete/<%=val._id%>" class="text-red-500 font-semibold">Delete</a>
                        </div>
                    </div>
                    <%})%>
                        <%} else{%>
                            <h1 class="text-2xl text-zinc-500 ml-2">No user found</h1>
                            <%}%>

        </div>
    </div>
</body>

</html>